# Đóng góp cho OS Quest 11

1. Tạo nhánh mới từ `main`.
2. Chạy `npm install`, `npm run test`, `npm run build` trước khi gửi thay đổi.
3. Nội dung mới phải phù hợp học sinh lớp 11, ngắn gọn, có phản hồi và không yêu cầu dữ liệu cá nhân thật.
4. Câu hỏi mới cần có đáp án, giải thích, gợi ý và phương án nhiễu hợp lí.
5. Không đưa secret key hoặc Supabase service role key vào repository.
